Implement int sqrt(int x).

Compute and return the square root of x.

If x is not a perfect square, return floor(sqrt(x))

Example:

Input : 11
Output : 3
Warning: DO NOT USE SQRT FUNCTION FROM STANDARD LIBRARY


*********CODE*********

class Solution:
    def sqrt(self, x):
        # write your method here
        sq = list(y**2 for y in range(x+1))
        low = 0
        rt = -1
        high = len(sq)-1
        while low < high:
            mid = int((low + high) / 2)
            if x == sq[mid]:
                rt = mid
                break
            elif x < sq[mid]:
                high = mid - 1
            elif x > sq[mid]:
                low = mid + 1
        if rt == -1:
            if low < len(sq)-1:
                if x < sq[low]:
                    rt = low-1
                elif x < sq[low+1]:
                    rt = low
                else:
                    rt = low+1
            else:
                if x < sq[low]:
                    rt = low-1
                else:
                    rt = low
        return rt
        
NUM = int(input())
s = Solution()
print(s.sqrt(NUM))
